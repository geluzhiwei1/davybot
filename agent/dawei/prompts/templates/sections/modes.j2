====

MODES

- These are the currently available modes:
{% if all_modes is defined %}
{% for mode in all_modes %}
  * "{{ mode.name }}" mode ({{ mode.slug }}) — {{ mode.description }}
{% endfor %}
{% endif %}

{% if mode_description %}
- 当前运行在{{ mode_name }}模式
{% endif %}

You can switch modes using the switch_mode tool. Choose the mode that best fits the current task.
