# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
.output
.vinxi
.tanstack/**
.nitro
*.local

# Lock files — npm (package-lock.json) is tracked: CI npm ci + setup-node cache 依赖它
pnpm-lock.yaml
bun.lockb
yarn.lock

# Wrangler / Cloudflare
.wrangler/
.dev.vars

# Tauri
src-tauri/ui-dist/
src-tauri/target/
src-tauri/gen/
src-tauri/icons/*.bak
src-tauri/binaries/

# Python
__pycache__/

# TypeScript incremental build cache
*.tsbuildinfo

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
# F4: env 出库 —— 仅中性模板/集成测试/demo 端点配置入库(.env.demo 全公开云端端点,无秘密;
# 不入库则换机构建 demo 即烤空 → 0922 登录 404 事故)
.env*
!.env.example
!.env.integration
!.env.demo
src-tauri/target-check/
