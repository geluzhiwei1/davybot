# Tauri environment configuration
# Used when building and running Tauri desktop app

# In Tauri mode, you typically connect to a remote server instead of localhost
# Change these URLs to point to your production backend server

# Example: Connect to local backend (for development)
VITE_API_BASE_URL=http://localhost:8465/api
VITE_WS_BASE_URL=ws://localhost:8465/ws

# Example: Connect to remote backend (for production)
# VITE_API_BASE_URL=https://your-server.com/api
# VITE_WS_BASE_URL=wss://your-server.com/ws
