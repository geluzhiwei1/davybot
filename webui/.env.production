# Production Environment Configuration
# 生产环境配置（默认值，可通过部署时覆盖）

# ==================== 日志配置 ====================
# 启用日志系统（true=启用, false=禁用）
VITE_LOGGING_ENABLED=true

# 日志级别: debug | info | warn | error
# 生产环境建议使用 warn，减少性能影响
VITE_LOG_LEVEL=warn

# 是否输出到浏览器控制台
# true=输出到控制台（即使被 tree-sharing，日志仍存储在内存）
# false=仅存储到内存（通过 logger.getLogs() 获取）
VITE_LOG_CONSOLE=true

# ==================== API 配置 ====================
# 是否记录 HTTP 请求日志（生产环境建议 false）
VITE_API_LOG_REQUESTS=false

# 是否记录 HTTP 响应日志（生产环境建议 false）
VITE_API_LOG_RESPONSES=false

# ==================== 调试配置 ====================
# 是否启用调试模式（生产环境建议 false）
VITE_DEBUG=false

# 是否暴露全局调试对象到 window（生产环境必须 false）
VITE_DEBUG_EXPOSE_GLOBAL=false

# ==================== DevTools 配置 ====================
# 是否自动打开 DevTools（Web 生产环境无效，仅 Tauri 有效）
VITE_DEVTOOLS_AUTO_OPEN=false

# ==================== WebSocket 配置 ====================
# 是否启用自动重连
VITE_WS_AUTO_RECONNECT=true

# 重连延迟（毫秒）
VITE_WS_RECONNECT_DELAY=3000

# 最大重连次数（0=无限重连）
VITE_WS_MAX_RECONNECT_ATTEMPTS=0

# ==================== API 端点 ====================
# 生产环境通常从相对路径访问，无需设置
# 如果需要连接到远程服务器，取消下面的注释：
# VITE_API_BASE_URL=https://your-server.com/api
# VITE_WS_BASE_URL=wss://your-server.com/ws
